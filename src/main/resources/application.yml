spring:
  datasource:
    url: jdbc:mysql://localhost:3306/electronics_shop?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


  # C?u hình ?? dùng @CreatedDate và @LastModifiedDate
  data:
    jpa:
      repositories:
        bootstrap-mode: default
